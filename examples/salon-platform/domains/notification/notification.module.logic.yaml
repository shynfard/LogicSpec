# yaml-language-server: $schema=../../../../schema/logicspec-0.1.schema.json
logicSpec: "0.1"

kind: module

module:
  id: notification
  name: Notification
  description: >
    Sends customer notifications in reaction to workspace events.
