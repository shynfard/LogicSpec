# yaml-language-server: $schema=../../schemas/events.schema.json
version: "1"

events:

  BookingCreated:
    topic: booking.created
    producer: booking
    consumers:
      - notification
      - calendar
    description: A booking has been successfully created.

  PaymentCompleted:
    topic: payment.completed
    producer: payment
    consumers:
      - booking
