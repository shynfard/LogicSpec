version: "1"

features:
  directory: .

catalogs:
  services: ./services.yaml
  events: ./events.yaml

output:
  directory: ./generated

render:
  view: flow
  direction: TD

# The customer and pubsub actors document responsibility without owning steps;
# silence the unused-actor info for this workspace.
diagnostics:
  LS402: "off"
