version: "1"

features:
  directory: .

catalogs:
  # Shared, reusable actors and step templates referenced from features via $ref.
  definitions: ./definitions.yaml

output:
  directory: ./.logicspec

render:
  view: flow
  direction: TD
